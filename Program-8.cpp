#include <iostream>
using namespace std;
int main()
{
    int n, sum = 0,i=1 ; //complete the declaration part

    std::cout << "Enter a positive integer: ";
    std::cin >> n;

    while(i<=n)
    {
        sum=sum+i;
        i++; //complete the missing code
        //complete the missing code
    }

    std::cout <<sum;
    return 0;
}
